# TODO - Streamlit Decision Tree Regressor App

- [ ] Create plan for Streamlit app (UI + model + evaluation + visualization)
- [ ] Implement `app.py` for decision tree regression using the provided CSV dataset
- [ ] Include data preprocessing (missing values, categorical encoding)
- [ ] Implement training/testing split and training of DecisionTreeRegressor
- [ ] Provide evaluation metrics (MAE, MSE, RMSE, R2)
- [ ] Add UI controls for hyperparameters (max_depth, min_samples_split, min_samples_leaf, criterion)
- [ ] Add plots: predicted vs actual and feature importance (if available)
- [ ] Add single-row prediction form
- [ ] Add README with run instructions (`pip install streamlit scikit-learn pandas matplotlib seaborn`)
- [ ] Create `requirements.txt`
- [ ] Test the app locally by running `streamlit run app.py`

